#include<stdio.h>
//0~32,94~126
int main(){
	int character[95]={0};
	int k;
	char ch;
	do{
		ch=getchar();
		k=ch - 32;
		if(character[k]==0){
			printf("%c",ch);
			character[k]++;
		}
	}while(ch!='\n');
	do{
		ch=getchar();
		k=ch - 32;
		if(character[k]==0){
			printf("%c",ch);
			character[k]++;
		}
	}while(ch!='\n');
	return 0;
}
