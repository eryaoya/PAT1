#include <stdio.h>
int main()
{
	//输入n
	int n=0;
	scanf("%d", &n);
	//count是砍的次数
	int count = 0;
	//while循环
	while (n != 1) {
		//if分支
		if (n % 2 == 0) {
			n = n / 2;
		}
		else {
			n = (3 * n + 1) / 2;
		}
		count++;
	}
	printf("%d", count);
	



	return 0;
}
